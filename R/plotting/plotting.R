library(ggplot2)

custom_forest_plot <- function(model, color_scheme = "default", show_weights = TRUE, ...) {
  # Implementation with more customization options
}

custom_funnel_plot <- function(model, color_scheme = "default", ...) {
  # Implementation with more customization options
}

custom_bubble_plot <- function(model, moderator, color_scheme = "default", ...) {
  # Implementation with more customization options
}