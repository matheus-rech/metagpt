source("R/utils/data_validation.R")
source("R/utils/error_handling.R")

library(metafor)

perform_sensitivity_analysis <- function(data, effect_size = "SMD", method = "REML") {
  # ... (rest of the function remains the same)
}