library(testthat)
source("setup.R")

test_that("convert_to_yi_vi works correctly", {
  # Add test for convert_to_yi_vi function
})

test_that("custom_forest_plot works correctly", {
  # Add test for custom_forest_plot function
})

test_that("custom_funnel_plot works correctly", {
  # Add test for custom_funnel_plot function
})

test_that("custom_bubble_plot works correctly", {
  # Add test for custom_bubble_plot function
})

# Add more tests for other helper functions